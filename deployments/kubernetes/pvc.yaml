apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: risk-assessor-data
  namespace: risk-assessor
  labels:
    app: risk-assessor
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # Uncomment and modify for specific storage class
  # storageClassName: standard
